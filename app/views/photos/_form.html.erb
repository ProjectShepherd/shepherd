<%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :missing_person %><br>
    <%= f.text_field :missing_person %>
    <%= f.text_field :mobile %>
    <%= f.text_field :web %>
    <%= f.text_field :thumb %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
